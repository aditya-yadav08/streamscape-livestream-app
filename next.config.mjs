/** @type {import('next').NextConfig} */
const nextConfig = {
    // Whether to enable strict mode for React.
    reactStrictMode: true,
  
    // Define other configurations here as needed.
  };
  
  export default nextConfig;